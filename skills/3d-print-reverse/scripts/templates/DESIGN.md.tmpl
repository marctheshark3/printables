# {{PART_NAME}}

Narrative only. The machine contract is `docs/PRINT_SPEC.yaml`. The rebuild input is `reverse/{{BODY}}.ir.json`. This file is never parsed.

The input STL is a reference mesh. Reconstruction uses sketches and features. Triangle-wrapped STEP is HARD.
