# {{NAME}}

Narrative notes only. `docs/PRINT_SPEC.yaml` is the machine contract.

- Product: {{NAME}}
- Class: {{PRODUCT_CLASS}}
- Backend: Blender 4.x headless bpy (`pblend` / `src/build.py`)
- Version: {{VERSION}}

Do not invent mechanical dimensions here. Named parameters, tolerances, and provenance belong in PRINT_SPEC.yaml.

```bash
pblend run --project .
pblend gate --project .
pblend pack --project . --version {{VERSION}}
```
