# Example coupon bracket

Narrative only. The machine contract is `PRINT_SPEC.yaml`.

Invented 40 × 30 × 12 mm envelope so the public repo can show a spec + OpenSCAD pair. Not a production fit.

Copy `src/bracket-coupon.scad` and change the named parameters in both the SCAD and the spec.
