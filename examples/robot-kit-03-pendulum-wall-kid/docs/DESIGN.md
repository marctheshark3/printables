# robot-kit-03-pendulum (wall)

Narrative only. The machine contract is `PRINT_SPEC.yaml`.

Same inverted pendulum as desk `03/`, hung on a wall. The plate has two M3 keyhole slots at the top and two M3 corner holes at the bottom. The stick swings **in front of the wall** in a vertical plane parallel to the wall. USB-C exits the bottom edge — not crushed against the wall.

Leave stick length + ball of clearance in the sweep; the occupancy gate is that proof.

Print: wall_plate, stick, knob (stick/knob match desk 03). Buy MCU, one SG90.

Desk sibling: `../03/`.
