# Example VibeCAD coupon bracket

Narrative only. The machine contract is `PRINT_SPEC.yaml`. VibeCAD chat and `.FCStd` cannot override it.

Same invented 40 × 30 × 12 mm envelope as `examples/bracket-coupon`, remade with VibeScript/Part CSG. Not a production fit.

Default CI does not run VibeCAD. The committed STL is the mesh gate; live OCC export is extra extra when `VIBECAD_CMD` is set.
